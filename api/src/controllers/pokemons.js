const {Pokemon, Type} = require('../db'); //Trae los modelos
const axios = require('axios')

const getPokemons = (req, res) => {

}

const getId = (req, res) => {
    
}

module.exports = {
    getPokemons,
    getId
}